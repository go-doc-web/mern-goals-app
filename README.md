# mern-goals-app

## npm

- npm install express
- npm install --save-dev nodemon <br>nodemon — это инструмент, который помогает разрабатывать
  приложения на основе Node.js, автоматически перезапуская приложение узла при обнаружении изменений
  файлов в каталоге.
- npm i dotenv //in JS file require('dotenv').config();
- npm install mongoose
- npm install colors
- npm install cors <br> Simple Usage (Enable All CORS Requests)<br> app.use(cors()); -npm i
  express-async-handler // щбработка try/catch <br> const asyncHandler =
  require('express-async-handler')

## .env

NODE_ENV = development <br> NODE_ENV = production // change one variant<br> PORT = 8000<br>
